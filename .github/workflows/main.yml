name: Create Release

on: 
  push:
    branches:
      - master

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
#       - name: Set up Go 1.13
#         uses: actions/setup-go@v1
#         with:
#           go-version: 1.13
#         id: go

      - name: Check out source code
        uses: actions/checkout@master

#       - name: Build
#         env:
#           ACCESS_TOKEN: ${{ secrets.GITHUB_ACTION_TOKEN }}
#         run: git config --global url."https://beyond5959:${ACCESS_TOKEN}@github.com".insteadOf "https://github.com" && CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o ones-third-importer main.go
        
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: a
          release_name: b
          body: |
            Changes in this Release
            - First Change
            - Second Change
          draft: false
          prerelease: false
     
#       - name: Compress and package
#         run: tar zcvf ./ones-third-importer.tar.gz ./conf ./ones-third-importer
        
#       - name: Upload Release Asset    
#         id: upload-release-asset 
#         uses: actions/upload-release-asset@v1.0.1
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
#           asset_path: ./ones-third-importer.tar.gz
#           asset_name: ones-third-importer.tar.gz
#           asset_content_type: application/x-gzip
